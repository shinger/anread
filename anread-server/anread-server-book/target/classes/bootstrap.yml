server:
  address: 127.0.0.1
  port: 8004
spring:
  application:
    name: anread-server-book
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        ip: 127.0.0.1
      config:
        server-addr: 127.0.0.1:8848
  config:
    import: optional:nacos:anread-server-book.yml?group=DEFAULT_GROUP
