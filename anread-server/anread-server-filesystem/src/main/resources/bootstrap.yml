server:
  address: 127.0.0.1
  port: 8002
spring:
  application:
    name: anread-server-filesystem
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        ip: 127.0.0.1
      config:
        server-addr: 127.0.0.1:8848
  config:
    import:
      - optional:nacos:anread-server-filesystem.yml?group=DEFAULT_GROUP
      - optional:nacos:minio-config.yml?group=DEFAULT_GROUP
